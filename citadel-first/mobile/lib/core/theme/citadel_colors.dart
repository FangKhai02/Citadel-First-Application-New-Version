import 'package:flutter/material.dart';

class CitadelColors {
  CitadelColors._();

  // Backgrounds
  static const Color background = Color(0xFF0C1829);
  static const Color surface = Color(0xFF0F172A);
  static const Color surfaceLight = Color(0xFF1E293B);
  static const Color surfaceHover = Color(0xFF2D3A4F);

  // Primary (Cyan/Teal from launcher_icon)
  static const Color primary = Color(0xFF29ABE2);
  static const Color primaryDark = Color(0xFF1A7BA8);
  static const Color primaryLight = Color(0xFF5CC4E8);

  // Accent
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);

  // Text
  static const Color textPrimary = Color(0xFFE2E8F0);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textMuted = Color(0xFF64748B);

  // Borders & Dividers
  static const Color border = Color(0xFF1E3A5F);
  static const Color divider = Color(0xFF1E3A5F);

  // Text (additional)
  static const Color textBody = Color(0xFFCBD5E1);

  // CTA Button Gradient
  static const Color ctaGradientTop = Color(0xFF2E6DA4);
  static const Color ctaGradientBottom = Color(0xFF1B4F7A);

  // Card
  static const Color cardBackground = Color(0xFF0F172A);
  static const Color cardBorder = Color(0xFF1E3A5F);
}